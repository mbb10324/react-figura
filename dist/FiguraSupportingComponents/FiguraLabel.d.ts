import { PropsWithChildren } from "react";
import React from "react";
interface Props extends PropsWithChildren {
    labelStyle?: any;
}
export default function FiguraLabel(props: Props): React.JSX.Element;
export {};
