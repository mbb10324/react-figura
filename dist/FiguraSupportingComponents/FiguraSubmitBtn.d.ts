import { PropsWithChildren } from "react";
import React from 'react';
interface Props extends PropsWithChildren {
    wrapper?: any;
    buttonStyle?: any;
}
export default function FiguraSubmitBtn(props: Props): React.JSX.Element;
export {};
