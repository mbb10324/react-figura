import { PropsWithChildren } from "react";
import React from "react";
interface Props extends PropsWithChildren {
    titleStyle?: any;
}
export default function FiguraTitle(props: Props): React.JSX.Element;
export {};
