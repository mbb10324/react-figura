import React from "react";

export const FiguraContext = React.createContext<any>(null);

export const ParentContext = React.createContext<any>("");

export const ResetContext = React.createContext<any>(null)

